//! Дымовой тест — проверяет, что проект собирается и тест-харнесс работает.
//! Реальные тесты (safety-матрица, тулы) добавляются по задачам плана фазы 1.

#[test]
fn smoke_builds_and_runs() {
    assert_eq!(2 + 2, 4);
}
